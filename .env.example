DATABASE_URL=postgresql://postgres:postgres@localhost:5432/kavanow
BASE_DOMAIN=lvh.me:5173
COOKIE_SECRET=change-me-to-a-random-string-at-least-32-chars

# SMTP (use Mailpit for local dev)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@kavanow.gr

# API dev port — change if 3000 conflicts with another local service (e.g. OrbStack/Gotenberg)
API_PORT=3000
