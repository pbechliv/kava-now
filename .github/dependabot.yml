version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Europe/Berlin
    open-pull-requests-limit: 10
    versioning-strategy: increase
    # Wait out pnpm's minimumReleaseAge (1d default) before opening bump PRs,
    # otherwise `pnpm install --frozen-lockfile` rejects too-fresh versions in
    # CI. Margin above 1d so weekend releases don't fail Monday's run.
    cooldown:
      default-days: 7
      semver-patch-days: 3
    labels:
      - dependencies
    commit-message:
      prefix: chore
      include: scope
    groups:
      react:
        patterns:
          - react
          - react-dom
          - react-router
          - "@types/react"
          - "@types/react-dom"
      tanstack:
        patterns:
          - "@tanstack/*"
      tailwind:
        patterns:
          - tailwindcss
          - "@tailwindcss/*"
      drizzle:
        patterns:
          - drizzle-orm
          - drizzle-kit
      better-auth:
        patterns:
          - better-auth
      hono:
        patterns:
          - hono
          - "@hono/*"
      vite-plus:
        patterns:
          - vite-plus
          - "@voidzero-dev/*"
      nodemailer:
        patterns:
          - nodemailer
          - "@types/nodemailer"
      types:
        patterns:
          - "@types/*"
        exclude-patterns:
          - "@types/react"
          - "@types/react-dom"
          - "@types/nodemailer"

  - package-ecosystem: docker
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Europe/Berlin
    labels:
      - dependencies
    commit-message:
      prefix: chore
      include: scope
    # The Node version is pinned in both the Dockerfile and .node-version, but
    # dependabot's docker ecosystem only sees the Dockerfile — bumping it alone
    # drifts the two. .node-version has no dependabot ecosystem, so Node bumps
    # are done by hand across both files. Caddy updates still flow through here.
    ignore:
      - dependency-name: "node"

  - package-ecosystem: docker-compose
    directories:
      - /
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Europe/Berlin
    labels:
      - dependencies
    commit-message:
      prefix: chore
      include: scope

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Europe/Berlin
    labels:
      - dependencies
    commit-message:
      prefix: chore
      include: scope
